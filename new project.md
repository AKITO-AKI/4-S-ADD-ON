# 三年次課題研究

## 2.5Dアニメ背景オートプロジェクター「SoloStudio-Proj(仮)」
### 要件定義・仮設計書（新方針）

## 1. 研究概要

### 1-1. 目的と基本思想
- **基本思想**  
  「人間の作業（カメラワーク・キャラ配置）」と「AIの作業（背景美術・ルック開発）」を完全分業する「一人アニメスタジオ」構想を継承する。  
- **目的**  
  Blender上のグレーボックス（背景アタリ）から、ローカルAIで特定画風の高品質背景を1枚生成し、対象3Dオブジェクトへ**カメラプロジェクションマッピング**を全自動構築するアドオンを実装する。  

### 1-2. 開発方針のシフト（ピボット背景）
- 先行事業のリリースにより、従来のV2V中心方針の独自性と実用価値が低下。  
- V2Vで発生しやすいフリッカーやモデル干渉リスクを回避するため、**静的シーン～緩やかなカメラ移動向けの2.5D方式**へ移行する。  

## 2. システムアーキテクチャ

| 役割 | ソフトウェア/技術 | 詳細 |
| :-- | :-- | :-- |
| フロントエンド | Blender (Python API) | ユーザー操作、3Dアタリ作成、Depth/Lineart出力、生成画像読み込み、投影用マテリアルノード自動構築 |
| バックエンド | ComfyUI (Local API) | Blenderからの要求を受け、ControlNet + IP-Adapter構成で高品質背景1枚を生成 |
| 通信 | HTTP (REST) / WebSocket | 生成開始指示、進捗通知、Blender UIフリーズ回避のための非同期監視 |

## 3. 使用AIモデル構成

- **ベースモデル**: Animagine XL 3.1 などのSDXLアニメ特化モデル  
- **空間制御**: ControlNet-XL Depth / Lineart  
  - グレーボックス形状とパースを厳密に保持  
- **画風固定**: IP-Adapter  
  - 参照画像のスタイルを背景生成結果に強制適用  

> ※ AnimateDiffは本計画では使用しない（1枚絵品質と空間整合性を優先）。

## 4. データパイプライン（自動処理フロー）

1. **Step 1: Blenderデータ抽出**  
   投影カメラ視点から、対象グレーボックスのDepth/Lineartパスを出力。  
2. **Step 2: API送信と非同期監視開始**  
   パス画像＋スタイル参照画像をJSON化してComfyUIへ送信。  
   Blender側はWebSocketで進捗監視し、メインスレッド停止を防止。  
3. **Step 3: ComfyUI背景生成**  
   ControlNet + IP-Adapterで、アタリ形状に一致した高品質背景1枚を生成。  
4. **Step 4: 自動プロジェクション構築（開発コア）**  
   生成画像をBlenderへ自動インポートし、対象オブジェクトへ投影マテリアルを自動作成。  
   ノード構成:  
   `Texture Coordinate (Window) → Mapping → Image Texture → Emission`  
   （UV展開なしでカメラ投影を成立させる）

## 5. UI更新方針（Nパネル: SoloStudio Director）

- **入力項目**
  - Prompt / Negative Prompt
  - Style Reference（IP-Adapter用画像パス）
  - Target Objects（投影対象のコレクションまたは複数オブジェクト）
- **表示項目**
  - 非同期進捗バー（進行率・処理状態）
- **実行ボタン**
  - `[Generate & Project Background]`

## 6. 完成までの実装工程

### Phase 0: 仕様確定・PoC設計
- 2.5D運用範囲（想定カメラ移動量）と検証指標を定義  
- ComfyUIワークフロー（ControlNet/IP-Adapter）入出力仕様を固定  

#### Phase 0 合意項目（先行確定）
- **2.5D運用範囲（PoC評価条件）**
  - カメラ操作: パン/ティルト/ドリーを対象とし、極端な回り込みショットは対象外
  - 検証対象: 「静的シーン〜緩やかなカメラ移動」を標準運用ケースとする
  - 破綻判定: 伸び/にじみ/遠近不整合が目視で明確なフレームを破綻として記録する
- **評価指標（PoCで必ず取得）**
  - 破綻発生率（ショット内フレーム比）
  - 再生成回数（1ショット完了までの試行回数）
  - 完走率（UIフリーズなしで生成〜投影まで到達した割合）
  - 生成〜投影完了までの処理時間（ショット単位）
- **ComfyUI入出力仕様（固定）**
  - 画像入力は `/upload/image` に multipart で先行アップロードする
  - アップロードで返却された `name` をワークフローJSONに埋め込み、`/prompt` に投入する
  - 進捗監視は WebSocket（`/ws`）で受信し、完了後は履歴APIで出力を取得する

#### Phase別 DoD（Definition of Done）
- **Phase 0 DoD**
  - 2.5D運用範囲・評価指標・除外ケースが文書化され、レビュー合意済み
  - ComfyUI入出力仕様（アップロード→キュー投入→進捗監視）が文書で固定済み
- **Phase 1 DoD**
  - 投影カメラ基準のDepth/Lineart出力が再現可能
  - Target Objects指定に従い、対象外オブジェクトが出力に混入しない
  - 未選択/出力先不正時に処理が中断され、ユーザーに理由が提示される
- **Phase 2 DoD**
  - REST投入、WebSocket進捗更新、再試行/中断が一連で動作する
  - 生成実行中もUI操作が継続でき、フリーズしない
- **Phase 3 DoD**
  - 生成画像の読み込みから投影マテリアル作成まで自動実行できる
  - 標準ノード構成（Texture Coordinate → Mapping → Image Texture → Emission）が再現される
- **Phase 4 DoD**
  - Nパネル上で入力→実行→進捗確認→エラー把握の導線が完結している
  - 初見ユーザーが手順書なしでも標準フローを完走できる
- **Phase 5 DoD**
  - パン/ティルト/ドリーの破綻限界が実測値として整理されている
  - 破綻条件と回避策が運用ガイドとして提示されている
- **Phase 6 DoD**
  - エラーハンドリング、ログ、再現手順、E2Eデモ結果が一式そろっている
  - 要件達成の根拠と研究考察が最終文書に反映されている

### Phase 1: Blender側データ抽出基盤
- 投影カメラ基準のDepth/Lineart出力処理を実装  
- Target Objects指定に基づく出力対象フィルタを実装  

### Phase 2: API連携・非同期実行基盤
- RESTによる生成ジョブ投入処理を実装  
- WebSocket進捗受信とUI更新（非同期）を実装  
- 通信失敗時の再試行・中断ハンドリングを実装  

### Phase 3: 生成画像の自動投影機能（中核）
- 生成画像の自動読み込み処理を実装  
- 対象オブジェクトへのマテリアル生成/ノード結線を自動化  
- Emissionベースの投影表示を標準化  

### Phase 4: UI統合
- SoloStudio Directorパネルへ入力項目・実行ボタン・進捗表示を統合  
- 操作順序に沿ったUI配置とエラーメッセージ整備  

### Phase 5: 品質検証（研究課題）
- パン/ティルト/ドリーの破綻限界を実測し、推奨運用範囲を定義  
- 破綻条件と回避ガイドを研究結果として整理  

### Phase 6: 最終仕上げ
- エラーハンドリング・ログ整備・再現手順を確定  
- デモシーンでE2E検証し、要件達成を確認  
- 卒業研究向け考察（2.5D方式の有効性と限界）を文章化  

## 7. 想定リスクと研究上の検証ポイント

1. **Blenderフリーズ**  
   非同期通信（asyncio / modalタイマー）で既存設計を流用し回避。  
2. **投影破綻（引き伸ばし）**  
   カメラの前進量・回り込み角に対する限界をPoCで定量化し、推奨使用範囲として明文化する。  
