三年次課題研究2.5Dアニメ背景オートプロジェクター「SoloStudio-Proj(仮)」 要件定義・仮設計書1. プロジェクトの目的と基本思想基本思想: 「人間の作業（カメラワーク・キャラ配置）」と「AIの作業（背景美術・ルック開発）」を完全に分業する「一人アニメスタジオ」の構築という初期目標を継承する。  目的: Blender上に配置した簡素なグレーボックス（背景のアタリ）から、ローカルAIを用いて特定の画風を反映した高品質な背景画像を生成し、元の3Dオブジェクトに対して「カメラプロジェクションマッピング」を全自動で構築するアドオンを開発する。ピボットの背景: 既存のV2V（Video to Video）技術におけるフレーム間のチラつき（フリッカー）やAIモデル同士の干渉による破綻リスクを根本から排除するため、静的シーンや緩やかなカメラワークに特化した2.5Dアプローチへ転換する。  2. システム・アーキテクチャ(全体構成)フロントエンド（Blender）とバックエンド（ComfyUI）を分離し、非同期通信で繋ぐ基本構造は維持する。  役割ソフトウェア/技術詳細フロントエンドBlender (Python API)ユーザー操作、3Dアタリの作成、参照用レンダーパスの出力。【新規】生成画像の読み込みとカメラ投影マテリアルノードの自動構築。  バックエンドComfyUI (Local API)ノードベースの画像生成環境。BlenderからのAPIリクエストを受け取り、高品質な1枚絵を生成。  通信プロトコルHTTP (REST) / WebSocket生成処理の指示と、Blenderのフリーズを防ぐための非同期進捗監視。  3. 使用AIモデルの選定 (構成の最適化)動画生成用モジュール（AnimateDiff）を外し、1枚絵の空間整合性と画風固定に特化する。ベースモデル: Animagine XL 3.1 等のSDXLアニメ特化モデル  空間制御モジュール: ControlNet-XL Depth / Lineart  役割: Blender上のアタリ（箱や床）の形状、パースペクティブをAIに厳密に守らせる。画風固定モジュール: IP-Adapter  役割: プロンプトに依存せず、ユーザーが指定したリファレンス画像（水彩調、サイバーパンク調などの背景画）のスタイルを生成画像に強制適用する。4. データ・パイプライン(処理の流れ)アドオン実行後、以下のフローが全自動で進行する。【Step 1: Blenderからのデータ抽出】現在のカメラ（投影用カメラ）からの視点で、対象となるグレーボックス群の depth (Z深度) と lineart (輪郭線) のパスを抽出・出力する。  【Step 2: APIリクエストと非同期通信開始】抽出したパス画像とスタイル参照画像をJSON化し、ComfyUIへ送信する。  Blender側はWebSocketで進捗監視を行い、メインスレッドのフリーズを防ぐ。  【Step 3: ComfyUIでの高画質生成】ControlNetとIP-Adapterが稼働し、アタリの形状に完全に一致した高品質な1枚の背景画像を生成する。【Step 4: プロジェクションの自動構築（※新規開発コア）】生成完了後、Blenderに画像を自動インポートする。Python APIを用いて、対象のグレーボックスに新規マテリアルを作成し、以下のノードツリーを自動で結線する。Texture Coordinate (Window) → Mapping → Image Texture (生成画像) → Emission (ライティングの影響を受けないようにするため)これにより、UV展開を一切行うことなく、カメラ視点からの完全な背景投影が完了する。5. UI (操作画面)のアップデート既存のNパネル「SoloStudio Director」をベースに、プロジェクション用の項目に最適化する。  入力項目:Prompt / Negative Prompt: 背景の具体的な指示。  Style Reference: 画風を固定するための画像パス指定（IP-Adapter用）。  Target Objects: プロジェクションを適用する対象オブジェクト（コレクション指定等）。情報表示:非同期処理によるProgress Bar表示。  実行ボタン: [Generate & Project Background]6. 開発における懸念事項と研究課題1. 既存のUX課題のクリア懸念と対策: API通信時のBlenderフリーズ問題については、すでに設計済みの非同期通信（asyncio / modalタイマー）の仕組みをそのまま流用することで解決可能。  2. プロジェクションマッピングの破綻限界（今後の検証課題）懸念: 1枚絵の投影であるため、初期の投影カメラ位置から「Z軸方向への極端な前進」や「側面の回り込み」を行うと、テクスチャが引き伸ばされて破綻する。対策: 今回の研究では「何度までのカメラ移動（パン・ティルト）、どれくらいの距離の移動（ドリー）なら視覚的に破綻しないか」をPoC（概念実証）期間に検証し、その限界値をアドオンの推奨使用範囲として定義する。これを卒業研究の考察に盛り込む。
